#include <stdio.h>

/*
Write a loop that prints all temperature reading stored in row i of the temperature array(Ex14). Use a pointer to visit each element of the row.
*/

int main(void)
{
	int temperature[7][24], *p, i;

	for (p = a[i]; p < a[i] + 24; p++)
	{
		printf("%d ", *p);
	}	

	return 0;
}

